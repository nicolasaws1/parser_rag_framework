# Backend (API de leitura) + ingestão — servidor SEM GPU
fastapi
uvicorn[standard]
supabase
python-dotenv
httpx
requests          # scripts/ (acervo, checar_rls, testar_acesso)
pymupdf           # contagem de páginas e conferência dos PDFs em acervo.py
